groups:
  - name: application-alerts
    rules:
      - alert: High5xxErrorRate
        expr: rate(http_requests_total{status=~"5.."}[1m]) > 0
        for: 30s
        labels:
          severity: warning
        annotations:
          summary: "High 5xx error rate"
          description: "The application is returning 5xx errors."